---
'@ankhorage/orchestrator-module-expo-localization': minor
---

Generate localization runtime files under the canonical `src/modules/localization` namespace and rename the generated provider to `LocalizationModuleProvider`. Add an optional package-owned host contribution with generic metadata, layout integration, config normalization, and a serializable admin schema while keeping the root Orchestrator module standalone.
